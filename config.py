"""
config.py — Central configuration for the Algo Trading system.
(Re-exports all settings and strategy parameters from the config/ package).
"""

from config.settings import *
from config.strategy_params import *
